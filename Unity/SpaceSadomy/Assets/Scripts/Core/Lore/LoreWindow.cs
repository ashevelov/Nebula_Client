namespace Game.Space.UI {
    public static class LoreWindow
    {

        public static void Show()
        {
			//UIEntity panel = UIManager.Get.GetLayout("lore_window");
			//panel.SetVisibility(true);
			//UILabel title = panel.GetChildrenEntityByName("title") as UILabel;
			//UILabel text = panel.GetChildrenEntityByName("text") as UILabel;

			//var loreData = DataResources.Instance.GetRandomLoreData();
			//title.SetText(loreData.title);
			//text.SetText(loreData.text);

			
			//UIManager.Get.RegisterEventHandler("LORE_WINDOW_CLOSE_CLICK", null, (evt) => {
			//	panel.SetVisibility(false);
			//});
		}

    }
}
